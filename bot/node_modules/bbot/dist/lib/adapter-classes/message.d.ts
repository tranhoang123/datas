import { Adapter } from './base';
import * as bot from '../..';
export declare abstract class MessageAdapter extends Adapter {
    name: string;
    abstract start(): Promise<void>;
    abstract shutdown(): Promise<void>;
    abstract dispatch(envelope: bot.Envelope): Promise<any>;
}
