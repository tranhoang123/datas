import * as bBot from '../..';
export declare abstract class Adapter {
    bot: typeof bBot;
    name: string;
    constructor(bot: typeof bBot);
    abstract start(): Promise<void>;
    abstract shutdown(): Promise<void>;
    parseSchema(internal: any, schema: {
        [path: string]: string;
    }, external?: any): any;
}
